# 公众号后台菜单配置指南：让"AI回复开关"按钮可点击

> 面向公众号后台维护者，无需编程知识。按本文操作约需 5 分钟。

## 一、背景：为什么需要这样配置

公众号后台自定义菜单里"发送消息（文字）"类型的按钮，点击后由微信**直接**把预设文字发给用户，**不会经过我们的服务器**——所以按钮本身无法开关 AI 问答。

解决办法：在按钮的文字内容里嵌入"蓝色链接"（简称**蓝字**）。用户点击按钮后收到的消息里带蓝字，**再点蓝字**即可开关 AI 问答。

## 二、配置步骤

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)，进入 **内容与互动 → 自定义菜单**（旧版入口：**功能 → 自定义菜单**）。
2. 找到"更多服务"下的二级按钮 **AI回复开关**，点击编辑。
3. 菜单动作保持 **发送消息**，内容类型选择 **文字**。
4. 将下方文案**完整复制**粘贴到文字输入框（包含 `<a ...>` 标签，一个字符都不要改）：

```
点击下方蓝色链接，开启或关闭 AI 问答：
<a href="weixin://bizmsgmenu?msgmenucontent=开启AI问答&msgmenuid=1">开启 AI 问答</a>
<a href="weixin://bizmsgmenu?msgmenucontent=关闭AI问答&msgmenuid=1">关闭 AI 问答</a>
```

5. 点击 **保存并发布**。
6. **手机端刷新菜单**：退出公众号会话重新进入；若菜单未更新，**取消关注后重新关注**即可强制刷新（新菜单有几分钟灰度延迟，属正常现象）。

## 三、验证清单

配置完成后，用手机依次验证：

- [ ] 点击菜单"AI回复开关"按钮 → 收到一条含**蓝色可点击文字**的消息
- [ ] 点蓝字"开启 AI 问答" → 收到回复"已开启 AI 问答，直接发送问题即可…"
- [ ] 随便发一个问题（如"上下班有班车吗"）→ 收到 AI 回答
- [ ] 再次点按钮，点蓝字"关闭 AI 问答" → 收到回复"已关闭 AI 问答…"
- [ ] 之后发问题 → 收到引导文案，不再走 AI

## 四、手动发送文字也可以开关（无需菜单）

用户直接在对话里输入以下文字，效果与点蓝字完全相同：

| 发送内容 | 效果 |
|---|---|
| `开启AI问答` | 开启（回复确认） |
| `关闭AI问答` | 关闭（回复确认） |
| `AI回复开关` | **一键切换**：当前开着就关、关着就开 |

## 五、常见问题排查

| 现象 | 原因与处理 |
|---|---|
| 蓝字显示为灰色纯文本、不可点击 | 后台保存时过滤了链接标签。删除内容重新粘贴，确认 `<a` 开头、`</a>` 结尾完整；标签内符号必须为**英文格式** |
| 点击按钮没有反应 / 菜单还是旧文案 | 菜单有灰度延迟（最长约 5 分钟）；取消关注重新关注强制刷新 |
| 点了蓝字没有收到确认回复 | 确认服务已部署最新版本；查看云托管控制台日志，应出现 `[wxmp] 收到推送` 与 `[wxmp] AI 开关切换` 记录 |
| 提示"AI 问答开启人数已达上限" | 开启人数达到系统上限（5000 人），稍后再试或联系管理员 |
| 保存时提示文字超限 | 粘贴内容不完整或混入了多余字符；清空后重新完整复制第二节文案 |

## 六、维护注意事项

- 蓝字链接的 `<a href="weixin://bizmsgmenu?msgmenucontent=...&msgmenuid=1">` 部分**不可修改**，否则开关失效；提示文字（"点击下方蓝色链接…"）可自由修改。
- `msgmenucontent=` 后面的内容（开启AI问答 / 关闭AI问答）必须与上面完全一致，服务端按此匹配命令。
- 修改菜单后务必点"保存并发布"，仅保存不发布不生效。
